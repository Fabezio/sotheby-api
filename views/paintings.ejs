<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>sotheby's api</title>
</head>
<body>
    <h1>sotheby's api</h1>
    <div class="views">

        <% for (paint of oeuvres) { %>
        <% function numStr(a, b) {
            a = '' + a;
            b = b || ' ';
            var c = '',
                d = 0;
            while (a.match(/^0[0-9]/)) {
              a = a.substr(1);
            }
            for (var i = a.length-1; i >= 0; i--) {
              c = (d != 0 && d % 3 == 0) ? a[i] + b + c : a[i] + c;
              d++;
            }
            return c;
          } %>
            <div class="border">
                <div class="thumbnail">
                    <img class="image" src="<%= paint.imgUrl %>" alt="mettre image">
                </div>
                <div class="description">
                    <strong class="col"><p><a href="paintings/<%= paint.name %>" ><%= paint.name %></a></p></strong>
                    <a class="col" href="paintings/<%= paint.author %>" ><%= paint.author %></a>
                    <p class="col" class="price"><%= numStr(paint.price) %> K€</p>
                    
                </div>
                        
                
                
            </div>
        <% } %>
    </div>
    <!-- <p>de la peinture? ou çà?</p> -->
    
</body>
</html>